#!/usr/bin/env ruby
# 実行可能ファイル: wolf_archiver

lib_dir = File.expand_path('../lib', __dir__)
$LOAD_PATH.unshift(lib_dir) unless $LOAD_PATH.include?(lib_dir)

require 'wolf_archiver'

WolfArchiver::WolfArchiverCLI.start(ARGV)
